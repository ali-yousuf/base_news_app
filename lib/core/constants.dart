import 'package:flutter/material.dart';

final navigatorKey = GlobalKey<NavigatorState>();

class Constants {
  static const String appName = "Base NewsApp";
  static Size defaultScreenSize = const Size(380, 720);
}
